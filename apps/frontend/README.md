# Frontend

Phase 0 provides a minimal React and TypeScript landing screen. It intentionally contains no
investigation dashboard functionality.

From this directory, install and run it with:

```bash
npm ci
npm run dev
```

Verification commands:

```bash
npm run format:check
npm run lint
npm test
npm run build
```
